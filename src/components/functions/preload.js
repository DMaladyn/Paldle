export default function preloadIcons(names) {
  const namesToPreload = names;

  /* namesToPreload.forEach((nameToPreload) => {
    const img = new Image();
    img.src = `iconsResized64/${nameToPreload}_64.png`;
  }); */
}
